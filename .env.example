# GitHub Actions Secrets
# Estas variáveis devem ser configuradas em: 
# Settings -> Secrets and variables -> Actions -> New repository secret

# Chave privada SSH para acesso ao servidor de produção (cleoselene.com)
# Deve corresponder a uma chave pública presente em ~/.ssh/authorized_keys no servidor.
SSH_PRIVATE_KEY="-----BEGIN OPENSSH PRIVATE KEY-----\n...\n-----END OPENSSH PRIVATE KEY---"

# --- Configurações de Deploy (Atualmente hardcoded no deploy.sh) ---
# Se desejar parametrizar o deploy.sh futuramente, estas seriam as variáveis:
SERVER_IP="cleoselene.com"
SERVER_USER="root"
REMOTE_DIR="/opt/cleoselene"

